---
apiVersion: v1
kind: Service
metadata:
  name: npm-pkguard
  labels:
    app: npm-pkguard
spec:
  selector:
    app: npm-pkguard
  ports:
    - name: http
      port: 18001
      targetPort: http
      protocol: TCP
  type: ClusterIP
