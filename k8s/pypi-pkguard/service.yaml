---
apiVersion: v1
kind: Service
metadata:
  name: pypi-pkguard
  labels:
    app: pypi-pkguard
spec:
  selector:
    app: pypi-pkguard
  ports:
    - name: http
      port: 18000
      targetPort: http
      protocol: TCP
  type: ClusterIP
