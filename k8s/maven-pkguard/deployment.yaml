---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: maven-pkguard
  labels:
    app: maven-pkguard
spec:
  replicas: 2
  selector:
    matchLabels:
      app: maven-pkguard
  template:
    metadata:
      labels:
        app: maven-pkguard
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: maven-pkguard
          image: ghcr.io/PKGuard/maven-pkguard:latest
          imagePullPolicy: IfNotPresent
          args: ["-config", "/etc/pkguard/config.yaml"]
          ports:
            - name: http
              containerPort: 18002
              protocol: TCP
          env:
            - name: PORT
              value: "18002"
            - name: PKGUARD_AUTH_USERNAME
              valueFrom:
                secretKeyRef:
                  name: maven-pkguard-auth
                  key: username
                  optional: true
            - name: PKGUARD_AUTH_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: maven-pkguard-auth
                  key: password
                  optional: true
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/pkguard
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
      volumes:
        - name: config
          configMap:
            name: maven-pkguard-config
