---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: npm-bulwark
  labels:
    app: npm-bulwark
spec:
  replicas: 2
  selector:
    matchLabels:
      app: npm-bulwark
  template:
    metadata:
      labels:
        app: npm-bulwark
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: npm-bulwark
          image: ghcr.io/Bulwark/npm-bulwark:latest
          imagePullPolicy: IfNotPresent
          args: ["-config", "/etc/bulwark/config.yaml"]
          ports:
            - name: http
              containerPort: 18001
              protocol: TCP
          env:
            - name: PORT
              value: "18001"
            - name: BULWARK_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: npm-bulwark-auth
                  key: token
                  optional: true
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/bulwark
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
      volumes:
        - name: config
          configMap:
            name: npm-bulwark-config
